#!/usr/bin/env ruby

require 'highline/import'
game_paths = Dir.glob('./*/game.rb')

choose do |menu|
  menu.prompt = 'Choose one of the following games:'
  menu.choices(*game_paths) do |choosen_path|
    exec "GAME_PATH=#{choosen_path} ruby visualize.rb"
  end
end

